# DAY04 検品記録 — 06_WEB/08_HTML Checklist

<!-- DAY04.md のレビュー方法「09_KNOWLEDGE_HUB/06_WEB/Checklist.md + 08_HTML/Checklist.md を1項目ずつ通す」の実施記録 -->

対象:`index.html` + `style.css`(2026-07-11時点)
方法:Playwrightによる機械チェック+目視レビュー。✗はその場で修正、修正不能な項目(実アセット・実URLが必要)は理由を明記して保留。

## 08_HTML/Checklist.md

| # | 項目 | 判定 | 備考 |
|---|------|:---:|------|
| 1 | h1は1つ・見出し階層は飛んでいないか | ✓ | h1×1、H1→H2→H3の順で飛びなし(Playwright確認済み) |
| 2 | main / nav / header / footer の大構造があるか | ✓(修正済み) | `<main>`が欠落していたため追加(header〜footer間の8セクションを包含)。navは単一ページLPのため対象外 |
| 3 | 全画像にalt/width/heightがあるか | N/A | `<img>`タグ自体が0件(すべてCSSベースのカード)。将来写真を追加する際に要対応 |
| 4 | リンクテキストは行き先が分かるか(「こちら」禁止) | ✓ | 全5リンクとも具体的な文言(「まずは相談してみる」「TEL 080-8414-5945」「会社概要」等)。「こちら」は不使用 |
| 5 | フォームのlabelは紐付いているか | N/A | `<form>`自体が0件(問い合わせはmailto:/tel:リンクで代替する設計) |
| 6 | title / description / OGP / canonical は設定済みか | ✗(一部保留) | title・description・og:type/title/descriptionは設定済み。**og:image(OGP画像)とcanonical URLが未設定**。どちらも実アセット・実URLが必要なため捏造せず保留(下記) |
| 7 | {{ }} プレースホルダの残りがないか | ✓ | 0件。【要記入】は`og:image`の1件のみ(上記と同一課題) |
| 8 | W3C validator(またはブラウザ検証)で致命的エラーがないか | ✓ | Playwrightでの重複ID検査0件・JSエラー0件。ブラウザパース時のhead/body異常なし |

### 保留項目の扱い(捏造しない)

- **OGP画像URL**:1200×630のシェア用画像がまだ存在しない。画像素材の準備後に差し込む
- **canonical URL**:このLPが最終的にどのURLで公開されるか(独自ドメイン配下のパス)が未確定。
  `http://trgger0307.com/`配下に公開するのか、別ドメインかも含めオーナー判断が必要なため、確定後に追加する
- **favicon**(06_WEBチェックリスト側の指摘、関連事項として併記):ロゴ・ファビコン素材が未作成

## 09_KNOWLEDGE_HUB/06_WEB/Checklist.md

| # | 項目 | 判定 | 備考 |
|---|------|:---:|------|
| 1 | スマホ実機で全ページ確認したか | △ | 実機ではなくPlaywrightで375px幅をエミュレート確認(横スクロールなし・崩れなし)。実機確認は別途推奨 |
| 2 | Lighthouseでパフォーマンス・アクセシビリティを確認したか | △ | Lighthouse CLI未導入のため未実施。代替として:コントラスト比は数値計算で検証済み(primary 4.70:1・muted 5.5:1、いずれもAA適合)、フォントは`preconnect`+`display=swap`で読み込み最適化済み、JS0件・画像0件のため初期表示は軽量 |
| 3 | 画像は圧縮・lazy loading済みか | N/A | 画像0件 |
| 4 | OGP・ファビコン・404を設定したか | ✗(保留) | OGP画像・faviconともに未設定(上記の通りアセット待ち)。404は未デプロイのため対象外 |
| 5 | フォームのテスト送信をしたか(スパム対策込み) | N/A | フォーム自体が存在しない設計(mailto:/tel:を採用)。両リンクのhref解決はPlaywrightで確認済み |
| 6 | HTTPSで表示されるか・リダイレクトは正しいか | N/A | 未デプロイ(ローカルファイル)。デプロイ時に確認する項目 |
| 7 | 解析(計測)は動いているか | ✗(未着手) | アクセス解析(GA等)は未設定。公開判断と合わせてオーナーが検討 |
| 8 | バックアップと復元手順があるか | ✓ | Gitでバージョン管理済み(コミット履歴が復元手順を兼ねる) |
| 9 | プレースホルダ({{ }}・仮画像)の残りがないか | ✓(ほぼ) | `{{ }}`は0件。仮画像(OGP)は1件残存 |

## 総括

- **修正済み**:`<main>`ランドマークの欠落
- **保留(実アセット・実URL待ち)**:OGP画像、favicon、canonical URL、アクセス解析
- **未デプロイのため対象外**:404設定、HTTPS確認、Lighthouse実測

上記「保留」4件は`07_CLIENTS/tnf-own/lp-v1`の外側(ドメイン・画像素材・解析タグの用意)に依存するため、
このプロジェクトフォルダ内の作業としてはこれ以上進められない。オーナー側での準備後に差し込む。
