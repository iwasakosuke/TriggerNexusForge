# DAY04 検品記録 — 06_WEB/08_HTML Checklist

<!-- DAY04.md のレビュー方法「09_KNOWLEDGE_HUB/06_WEB/Checklist.md + 08_HTML/Checklist.md を1項目ずつ通す」の実施記録 -->

対象:`index.html` + `style.css`(2026-07-12時点。保留4件のうち3件を解消済み)
方法:Playwrightによる機械チェック+目視レビュー。✗はその場で修正、修正不能な項目(実アセット・実URLが必要)は理由を明記して保留。

## 保留4件の解消(2026-07-12)

| 項目 | 対応 |
|------|------|
| OGP画像 | `assets/ogp.jpg`(1200×630)を新規デザイン・生成。TNFブランド(ネイビー×オレンジ)+キャッチコピーで構成。テキストコントラストも数値検証済み(バッジ8.23:1・サブ9.13:1)。PNG(265KB)→JPEG品質88(47.6KB)に変換し82%軽量化(文字の視認性は劣化なしを目視確認) |
| favicon | `assets/favicon.svg` + `favicon-32.png` + `apple-touch-icon.png` + `icon-512.png` を生成(FVヘッダーと同じ稲妻アイコン、ネイビー地×オレンジ) |
| canonical URL | オーナー確定(2026-07-12):`http://trgger0307.com/lp/` |
| アクセス解析 | オーナー確認(2026-07-12):現時点で導入しない(公開後に検討)。恒久保留ではなく「今回のスコープ外」として確定 |

## 08_HTML/Checklist.md

| # | 項目 | 判定 | 備考 |
|---|------|:---:|------|
| 1 | h1は1つ・見出し階層は飛んでいないか | ✓ | h1×1、H1→H2→H3の順で飛びなし(Playwright確認済み) |
| 2 | main / nav / header / footer の大構造があるか | ✓(修正済み) | `<main>`が欠落していたため追加(header〜footer間の8セクションを包含)。navは単一ページLPのため対象外 |
| 3 | 全画像にalt/width/heightがあるか | N/A | `<img>`タグ自体が0件(すべてCSSベースのカード)。OGP/favicon画像は`<link>`/`<meta>`のためalt対象外。将来LP内に写真を追加する際に要対応 |
| 4 | リンクテキストは行き先が分かるか(「こちら」禁止) | ✓ | 全5リンクとも具体的な文言(「まずは相談してみる」「TEL 080-8414-5945」「会社概要」等)。「こちら」は不使用 |
| 5 | フォームのlabelは紐付いているか | N/A | `<form>`自体が0件(問い合わせはmailto:/tel:リンクで代替する設計) |
| 6 | title / description / OGP / canonical は設定済みか | ✓ | title・description・og:type/url/title/description/image・twitter:card/image・canonical、すべて設定済み(Playwrightでcanonical 1件・favicon 2件を確認) |
| 7 | {{ }} プレースホルダの残りがないか | ✓ | `{{ }}`・【要記入】ともに0件(全文検索で確認) |
| 8 | W3C validator(またはブラウザ検証)で致命的エラーがないか | ✓ | Playwrightでの重複ID検査0件・JSエラー0件。ブラウザパース時のhead/body異常なし |

## 09_KNOWLEDGE_HUB/06_WEB/Checklist.md

| # | 項目 | 判定 | 備考 |
|---|------|:---:|------|
| 1 | スマホ実機で全ページ確認したか | △ | 実機ではなくPlaywrightで375px幅をエミュレート確認(横スクロールなし・崩れなし)。実機確認は別途推奨 |
| 2 | Lighthouseでパフォーマンス・アクセシビリティを確認したか | △ | Lighthouse CLI未導入のため未実施。代替として:コントラスト比は数値計算で検証済み(primary 4.70:1・muted 5.5:1、いずれもAA適合)、フォントは`preconnect`+`display=swap`で読み込み最適化済み、JS0件・LP内画像0件のため初期表示は軽量 |
| 3 | 画像は圧縮・lazy loading済みか | ✓ | LP内画像は0件(N/A)。OGP画像はJPEG品質88で47.6KBに圧縮済み(元PNG比82%減、視認性劣化なし) |
| 4 | OGP・ファビコン・404を設定したか | ✓ | OGP・favicon設定済み(上記)。404は未デプロイのため対象外 |
| 5 | フォームのテスト送信をしたか(スパム対策込み) | N/A | フォーム自体が存在しない設計(mailto:/tel:を採用)。両リンクのhref解決はPlaywrightで確認済み |
| 6 | HTTPSで表示されるか・リダイレクトは正しいか | N/A | 未デプロイ(ローカルファイル)。デプロイ時に確認する項目 |
| 7 | 解析(計測)は動いているか | 確定(未導入) | オーナー確認済み・現時点でスコープ外(上記) |
| 8 | バックアップと復元手順があるか | ✓ | Gitでバージョン管理済み(コミット履歴が復元手順を兼ねる) |
| 9 | プレースホルダ({{ }}・仮画像)の残りがないか | ✓ | 0件 |

## 総括

- **修正済み**:`<main>`ランドマークの欠落、OGP画像、favicon、canonical URL
- **オーナー判断で確定**:アクセス解析は今回スコープ外
- **未デプロイのため対象外**:404設定、HTTPS確認、Lighthouse実測
- **公開前に推奨**:スマホ実機での最終確認
